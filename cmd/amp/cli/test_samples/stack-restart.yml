stack-restart:
  cmd: amp stack start
  args:
    - stack1
  options:
  expectation: ([a-z0-9]{64})
stack-list:
  cmd: amp stack ls
  args:
  options:
  expectation: ((NAME)(\s)+(ID)(\s)+(STATE)((.)|(\s))+(stack1)(\s)+([a-z0-9]{64})(\s)+(Running)((.)|(\s))(.)*)
