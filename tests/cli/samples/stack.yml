- stack-create:
  cmd: amp stack up
  args:
     - "{{call .uniq `stack`}}"
  options:
     - -c ../../api/rpc/stack/test_samples/sample-01.yml
  expectation: stack-creating-service
  timeout: 20s

- stack-list:
  cmd: amp stack ls
  args:
    -
  options:
    -
  expectation: stack-list-running

- stack-remove:
  cmd: amp stack rm
  args:
    - "{{call .uniq `stack`}}"
  options:
    -
  expectation: stack-removing-service


- stack-list:
  cmd: amp stack ls
  args:
    -
  options:
    -
  expectation: stack-list-unavailable

- stack-list:
  cmd: amp stack ls
  args:
    -
  options:
    -
  expectation: stack-list-unavailable