# data

The `data` package contains backend service clients. Most of the packages
are only for fetching data from various databases (such as logs and metrics).
The `data.storage` package contains the interface for accessing `amp`'s
primary key/value storage (`etcd`).
